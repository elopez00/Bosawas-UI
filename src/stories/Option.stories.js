import React from 'react'
import { Option } from '../components'

export default {
    title: "Inputs/Option",
    component: Option
}

const Template = args => (
    <Option>Hello</Option>
)

export const OptionComponent =  Template.bind({})